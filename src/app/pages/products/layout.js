export const metadata = {
  title: "Fashion All Products",
  description: "all fashion wear available at lowest price",
};

function productsLayout({ children }) {
  return <>{children}</>;
}

export default productsLayout;
